name: rdj_scanner
description: Aplikasi Scanner OBD2 Motor Proyek RdJ
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bluetooth_serial: ^0.4.0
  permission_handler: ^11.3.1
  path_provider: ^2.1.2
  csv: ^6.0.0
  wakelock_plus: ^1.1.4
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
